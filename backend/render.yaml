services:
  - type: web
    name: terminal-chat-signaling
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn signaling_server:app --host 0.0.0.0 --port 10000"
    plan: free
